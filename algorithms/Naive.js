class Naive {
    constructor(width, height) {
        self.historyNodeChecked = []
        self.width  = width;
        self.height = height;
    }

    /* return array w/ CasesType.visualier position to add to map.arr */
    nextTurn(map) {
        let isFree = false;
        let r = -1;
        while(!isFree) {

            r = Math.floor(Math.random() * (4));
        }
        
    }
}